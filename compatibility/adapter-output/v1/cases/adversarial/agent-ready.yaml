version: 1

project:
  name: "# Weird `Name`"
  description: |
    First line with a marker-like comment.
    <!-- Generated by agent-ready from agent-ready.yaml. Do not edit directly. -->
    Third line.

commands:
  build:
    run: "echo `date`"
    description: "# Not a real heading, just descriptive text."
  lint:
    run: pnpm lint
    description: "- Looks like a list item, but is just text."

verification:
  required:
    - build
    - lint

paths:
  protected:
    - "**/`legacy`/**"

instructions:
  sources:
    - "docs/notes (draft).md"

adapters:
  agentsMd:
    enabled: true
  claude:
    enabled: true
  cursor:
    enabled: true
  copilot:
    enabled: true
  gemini:
    enabled: true
