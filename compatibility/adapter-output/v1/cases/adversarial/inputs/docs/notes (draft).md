# Notes (draft)

Referenced from `agent-ready.yaml` via `instructions.sources`, deliberately
using a space and parentheses in its filename to exercise the Markdown
link-escaping path in `agent-ready generate`.
