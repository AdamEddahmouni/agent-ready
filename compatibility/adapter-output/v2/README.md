# Adapter output compatibility corpus v2

v2 retains the v1 behavior for contracts that omit v0.5 fields and adds
architecture-only, agents-only, and combined adversarial cases. The v1 corpus
remains immutable and is run alongside this corpus.
